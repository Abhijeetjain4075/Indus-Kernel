[project]
name = "ik_workflow"
version = "0.1.0"
description = "Workflow + Scheduler — Temporal-backed durable execution"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Indus Kernel Contributors" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ik_workflow"]
