[project]
name = "ik_improvement"
version = "0.1.0"
description = "Self-Improvement Engine — GEPA, GRPO, distillation, A/B testing"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Indus Kernel Contributors" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ik_improvement"]
