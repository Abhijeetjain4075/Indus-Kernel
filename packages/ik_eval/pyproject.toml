[project]
name = "ik_eval"
version = "0.1.0"
description = "Evaluation Engine + Benchmark Engine — LLM-as-judge, regression, A/B"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Indus Kernel Contributors" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ik_eval"]
