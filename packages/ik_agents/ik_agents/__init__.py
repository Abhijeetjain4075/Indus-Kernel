"""ik_agents — Indus Kernel Agent Orchestrator.

The agent orchestrator coordinates multi-agent LLM collaboration.
Backed by LangGraph (production) with adapters for AutoGen, CrewAI, and smolagents.
A2A v1.0 native for inter-agent communication.

For M0 (skeleton), only the hello-world agent is implemented.
Full topologies (chain, graph, broadcast, consensus, GoA) come in M3.
"""

__version__ = "0.1.0"
