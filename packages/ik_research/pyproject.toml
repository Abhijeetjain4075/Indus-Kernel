[project]
name = "ik_research"
version = "0.1.0"
description = "Autonomous Research Engine — hypothesis → search → experiment → reflect loop"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Indus Kernel Contributors" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ik_research"]
