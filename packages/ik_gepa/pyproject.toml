[project]
name = "ik_gepa"
version = "0.1.0"
description = "GEPA Optimiser — Genetic-Pareto reflective prompt evolution (ICLR 2026 Oral)"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Indus Kernel Contributors" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ik_gepa"]
