[project]
name = "ik_protocols"
version = "0.1.0"
description = "Protocol Gateway — MCP 2026-07-28 server+client, A2A v1.0 server+client"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Indus Kernel Contributors" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ik_protocols"]
